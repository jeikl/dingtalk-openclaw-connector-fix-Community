# Release Notes — v0.8.21-fix49（2026-07-21）

## 安装

```bash
npx -y @jeik/dingtalk-connector install --force
openclaw gateway restart
```

## 本版要点

- 默认流式卡：`0d2c84b3-12c1-473b-b14a-f329a7a102cd.schema`，`cardContentVar=content`
- 含 fix48：`messageAnswerCard`（默认开）、`file://` 本地图修复、图片发送全路径增强
- 文档：最小/最大化配置、图片能力说明

从 fix48 直接 `--force` 安装即可。
